# Copiá este archivo como android/keystore.properties y completalo localmente.
# NO subas android/keystore.properties ni el archivo .jks/.keystore real.

storeFile=../private-keystores/aura-calendar-release.jks
storePassword=REPLACE_WITH_STORE_PASSWORD
keyAlias=aura-calendar
keyPassword=REPLACE_WITH_KEY_PASSWORD
