import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.adryrc13.auracalendar',
  appName: 'Aura Calendar',
  webDir: 'dist',
};

export default config;
